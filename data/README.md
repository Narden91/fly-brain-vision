# Data

`build_circuit.py` caches the official optic-column spreadsheet in `raw/` and writes
the offline circuit matrix plus metadata here. These files are not generated by the app.

MaleCNS-derived data are CC-BY and retain their upstream attribution requirements.
